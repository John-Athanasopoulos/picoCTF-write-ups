dont-use-client-side - write up

We open the url given to us and we find a login system protected by some kind of program.
We enter the inspector (Ctrl+Shift+I) and head to the Debugger tab. There we will find
under 2019shell1.picoctf.com/problem(32259)/(index) some html code.

We are interested in the Javascript code though. As we can see, the lock is protected by
some nested ifs, like the following:

if (checkpass.substring(0, split) == 'pico') {
      if (checkpass.substring(split*6, split*7) == 'b956') {
        if (checkpass.substring(split, split*2) == 'CTF{') {
         if (checkpass.substring(split*4, split*5) == 'ts_p') {
          if (checkpass.substring(split*3, split*4) == 'lien') {
            if (checkpass.substring(split*5, split*6) == 'lz_e') {
              if (checkpass.substring(split*2, split*3) == 'no_c') {
                if (checkpass.substring(split*7, split*8) == 'b}') {
                  alert("Password Verified")

If we take every part and try to sort them (in the order I did), we will get the flag:

picoCTF{no_clients_plz_eb956b}
